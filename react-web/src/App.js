import React from "react";

// Import necessary components
import Layout from "./components/layout/Layout";

const App = ({content, vCenter, currentPage}) => {
    return (
        <Layout/>
    );
};

export default App;
